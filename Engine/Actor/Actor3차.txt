Actor 클래스
- Color enum 클래스가 생성됨에 따라 Actor 클래스의 생성자의 파라미터에 color가 들어가게 변경되었다

GetSortingOrder 함수
- Console Game Engine에서 고민해야 할 문제이다
- 움직이는 두 개체가 한 좌표에 머무르게 된다면 어떻게 처리해야 될까?
- 아쉽게도 겹쳐 표현하는 것은 불가능에 가까울 것이다.(가능하다 하더라도 인력 낭비일 것으로 보임) 따라서 무엇이 더 중요한지, 무엇을 맨 꼭데기 레이어에 표현해야 할지 정해야 한다.

SetPosition 함수
- 기존에 있던 (x, y) 좌표를 SetConsolePosition으로 으로 지정하여 그렸던 작업을 Renderer 클래스가 담당하게 되며 해당 함수는 깔끔하게 정리되었다. 단일책임!

추가된 멤버 변수
- windows api로 인하여 console에서 다양한 색상을 사용할 수 있게 되었으므로 색상 변수가 생겼다
- 오너쉽(... 왜 있는거야..?) 가 생겼다
- 그리기 우선 순위를 갖는 sortingOrder가 생겼다

오너쉽
- 이 오너십에 대한 함수가 두 개가 생겼다 그것은 바로 추가와 읽기이다
- 왜.. 있는거지..